{% extends '::base.html.twig' %}

{% block body %}
    <h2>Nouveau besoin</h2>
    
    <div class="clear"></div> <br>
    
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" class="btn btn-primary" />
    {{ form_end(form) }}

    <hr>
    <a href="{{ path('mes_besoins') }}" class="btn btn-primary">Revenir</a>
{% endblock %}
